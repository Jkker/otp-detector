[tools]
android-sdk = "1.0"
java = "21.0.2"
gradle = "8.14.4"
kotlin = "2.1.21"

[env]
ANDROID_HOME = "{{env.HOME}}/android-sdk"

[tasks.build]
description = "Build the Release APK (match CI)"
run = "./gradlew assembleRelease"

[tasks.debug]
description = "Build the Debug APK"
run = "./gradlew assembleDebug"

[tasks.test]
description = "Run all unit tests"
run = "./gradlew test"

[tasks.lint]
description = "Run lint checks"
run = "./gradlew lintDebug"

[tasks.clean]
description = "Clean the build directory"
run = "./gradlew clean"

[tasks.release]
description = "Tag version for release (triggers CI)"
run = "git tag v$(grep 'versionName =' app/build.gradle.kts | cut -d'\"' -f2) && git push origin --tags"
